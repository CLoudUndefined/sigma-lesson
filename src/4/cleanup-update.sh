# Доработать так, чтобы старый бэкап удалялся только когда бэкапов
# накопилось 5 и больше, а не при каждом запуске. Подсказка - понадобится:
#   COUNT=$(ls ~/backups | wc -l)
#   if [ "$COUNT" -ge 5 ]; then
#       ... удаление ...
#   fi

